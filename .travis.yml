sudo: required
services:
    - docker
    
before_install:
    - docker build -t docker88/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true docker88/docker-react npm run test 
    